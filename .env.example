# MongoDB connection string (for MongoDB Compass / local)
MONGODB_URI=mongodb://localhost:27017/community_traffic_violation_db

# Server port
PORT=4000

# Postmark API token (keep this secret, do NOT commit real value)
POSTMARK_API_TOKEN=your_postmark_api_token_here